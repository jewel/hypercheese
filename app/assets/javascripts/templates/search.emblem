= input value=q placeholder="Search"

p
  | Count:
  = model.count

p
  label
    input type="checkbox" checked=selecting class="toggle"
    | Selection Mode

.results style=resultsStyle
  .viewport style=viewPortStyle
    = each item in viewPortItems
      = item-square-display itemId=item.model.id selected=item.selected imageClick="imageClick"
